<!--components/timeSelector/timeSelector.wxml-->
<van-popup show="{{ show }}" position="bottom" bind:close="onCancel" filter="{{ filter }}">
  <van-datetime-picker value="{{ currentTime }}"
  type="time"
  filter="{{ filter }}" bind:confirm="onConfirm" bind:input="onInput"
  />
</van-popup>
