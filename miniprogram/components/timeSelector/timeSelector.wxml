<!--components/timeSelector/timeSelector.wxml-->
<van-popup show="{{ show }}" position="bottom" bind:close="onClose" filter="{{ filter }}">
  <van-datetime-picker
  type="time"
  value="{{ currentDate }}"
  filter="{{ filter }}"
  />
</van-popup>
